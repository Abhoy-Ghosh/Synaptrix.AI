Phase 0 → Setup
Phase 1 → Backend Core
Phase 2 → AI Pipeline
Phase 3 → Semantic + FAISS
Phase 4 → DB + Auth + Credits
Phase 5 → Frontend
Phase 6 → Docker
Phase 7 → Deployment
Phase 8 → Optimization


pip install fastapi uvicorn python-dotenv
pip install requests numpy faiss-cpu
pip install sentence-transformers
pip install sqlalchemy psycopg2-binary
pip install python-jose passlib[bcrypt]

ctrl+shift+` 

cd Backend
source venv/bin/activate
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload


pkill -f uvicorn
pkill -f python


cd /home/abhoy/.cache/huggingface/ #model cached here

User Query
→ arXiv Fetch
→ Embeddings (MiniLM)
→ Cosine Ranking
→ Top Papers


cache
Query → lowercase → key
↓
Saved in JSON file
↓
Next time → fetched instantly